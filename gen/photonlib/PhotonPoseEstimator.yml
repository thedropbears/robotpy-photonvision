---

extra_includes:
- cvnp/cvnp.h

enums:
  PoseStrategy:
classes:
  EstimatedRobotPose:
    attributes:
      estimatedPose:
      timestamp:
      targetsUsed:
      strategy:
    methods:
      EstimatedRobotPose:
  PhotonPoseEstimator:
    methods:
      PhotonPoseEstimator:
        overloads:
          frc::AprilTagFieldLayout, PoseStrategy, frc::Transform3d:
      GetFieldLayout:
      GetPoseStrategy:
      SetPoseStrategy:
      SetMultiTagFallbackStrategy:
      GetReferencePose:
      SetReferencePose:
      GetRobotToCameraTransform:
      SetRobotToCameraTransform:
      SetLastPose:
      Update:
        overloads:
          "":
          const PhotonPipelineResult&:
          const PhotonPipelineResult&, std::optional<PhotonCamera::CameraMatrix>, std::optional<PhotonCamera::DistortionMatrix>:
      GetCamera:
