---

enums:
  PoseStrategy:
classes:
  EstimatedRobotPose:
    attributes:
      estimatedPose:
      timestamp:
      targetsUsed:
      strategy:
    methods:
      EstimatedRobotPose:
  PhotonPoseEstimator:
    methods:
      PhotonPoseEstimator:
        overloads:
          frc::AprilTagFieldLayout, PoseStrategy, PhotonCamera&&, frc::Transform3d:
          frc::AprilTagFieldLayout, PoseStrategy, frc::Transform3d:
      GetFieldLayout:
      GetPoseStrategy:
      SetPoseStrategy:
      SetMultiTagFallbackStrategy:
      GetReferencePose:
      SetReferencePose:
      GetRobotToCameraTransform:
      SetRobotToCameraTransform:
      SetLastPose:
      Update:
        overloads:
          "":
          const PhotonPipelineResult&:
          const PhotonPipelineResult&, std::optional<cv::Mat>, std::optional<cv::Mat>:
      GetCamera:
