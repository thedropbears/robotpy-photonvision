---

enums:
  PoseStrategy:
classes:
  RobotPoseEstimator:
    force_type_casters:
      - units::millisecond_t
    typealias:
      - map_value_type = std::pair<std::shared_ptr<PhotonCamera>, frc::Transform3d>
    methods:
      RobotPoseEstimator:
      Update:
      SetPoseStrategy:
        ignore: true  # missing
      SetReferencePose:
      SetLastPose:
      SetCameras:
      GetPoseStrategy:
      GetLastPose:
      GetReferencePose:
