---

classes:
  PhotonPipelineResult:
    shared_ptr: true
    methods:
      PhotonPipelineResult:
        overloads:
          "":
          units::second_t, wpi::span<const PhotonTrackedTarget>:
      GetBestTarget:
      GetLatency:
      HasTargets:
      GetTargets:
      operator==:
      operator!=:
